# Demo rehearsal -- the five-minute take (SAMSARA_VISION.md section 7)

Dry run through the real dispatch path (session_modes control words, the command registry, each command's argument grammar). Nothing was executed. Generated by tools/demo_rehearsal.py; re-run it after every change.

Catalog: 479 commands (287 builtin, 192 plugin). Hands-free session entry lane: dictate.

Result: 1 WORKS, 2 PARTIAL, 5 MISSING of 8 steps.

| step | utterance | resolves to | mode required | status | what is missing (owner) |
|---|---|---|---|---|---|
| 1 | wake up samsara | no wake phrase matches (configured: jarvis, hey jarvis, computer, hey computer, samsa, hey samsa) | asleep | MISSING | 'wake up samsara' is not a wake phrase (needs an openWakeWord model + wake_word_config.phrase), and even a matching wake word opens a one-command window, not the latched hands-free session -- a 'wake word opens the toggle session' bridge does not exist -- **dictation.py (_process_wake_command / enter_command_mode) + wake_word_config** |
| 2 | play something from my alternative rock playlist | 'play' (SMTC 'play' to whatever media session is current; the words after 'play' are ignored) | command | PARTIAL | in the dictate lane this is staged as dictation until 'end' (typed into the focused app); say 'command mode' first; also: 'play <anything>' resumes the current media session instead of choosing music; a playlist name is dropped on the floor -- **plugins/commands/music.py (handle_play / handle_media_play)** |
| 3 | put warp on the left screen and claude on the right | 'send' (window 'warp' -> destination 'left screen and claude on the right' (unparseable)) | command | MISSING | destination grammar knows only 'tv', 'here' and 'monitor N' -- not 'left/right screen'; a compound 'X on the left and Y on the right' is not split into two moves -- **plugins/commands/windows.py (_parse_destination / handle_send)** |
| 4 | tell claude the mode switch fix landed and ask what's next | no registered command | command | MISSING | no 'tell <app> <text>' capability: needs focus-the-app, dictate the text and submit as one command (today: 'focus claude', 'dictate <text>', 'end', 'submit' -- four utterances) -- **plugins/commands/app_verbs.py (focus) + samsara/session_modes.py (dictate lane) -- a new 'tell' verb** |
| 5 | The mode switch fix landed this afternoon and the hands f... | 40 words staged in the dictate lane, committed on 'end' | dictate | PARTIAL | needs Obsidian focused ('focus obsidian' works in the lane) and the commit word 'end'; 'verbatim' is not guaranteed: the commit re-decodes the joined audio and applies formatting tokens (verbatim profile exists only for terminals/URL bars) -- **samsara/session_modes.py (_commit_dictate_buffer) / docs/VERBATIM_PROFILE.md** |
| 6 | correct that | in dictate: typed into the focused app as text | ava (only grammar that knows 'correct that') | MISSING | no command edits the word just typed: the only 'correct that to Y' grammar is Ava's vocabulary teaching (teach_patterns.parse_correction_add), which persists a future transcription fix and does not touch the text on screen -- **plugins/commands/text_marker.py (select/retype the last word) with the grammar from samsara/teach_patterns.py** |
| 7 | scratch that | session_modes 'scratch that' -> pops the last unit of work (staged action, committed dictation chunk or command) | any latched mode | WORKS |  |
| 8 | go to sleep | in dictate: typed into the focused app as text | any latched mode | MISSING | 'go to sleep' is not a session exit phrase (those are: stop listening, exit hands free, exit command mode); add it to command_mode.abort_phrases in config, or to GLOBAL_SESSION_EXIT_PHRASES -- **samsara/session_modes.py (GLOBAL_SESSION_EXIT_PHRASES) / config command_mode.abort_phrases** |

## Reading the table

* The latched hands-free session (command-mode toggle tap) enters in the DICTATE lane. In that lane an utterance only dispatches as a command when it is a reserved exact command (or a curated prefix such as 'focus <x>'); everything else is staged as dictation and typed into the focused app on 'end'. The take as written contains no switch word, so steps 2-4 and 6 would be typed, not performed.
* 'mode required' is the lane in which the line resolves; 'resolves to' is what the session would actually do in the lane it is in.
* MISSING names the plugin or module that would own the capability.

Owners of every MISSING step:

* step 1: dictation.py (_process_wake_command / enter_command_mode) + wake_word_config
* step 3: plugins/commands/windows.py (_parse_destination / handle_send)
* step 4: plugins/commands/app_verbs.py (focus) + samsara/session_modes.py (dictate lane) -- a new 'tell' verb
* step 6: plugins/commands/text_marker.py (select/retype the last word) with the grammar from samsara/teach_patterns.py
* step 8: samsara/session_modes.py (GLOBAL_SESSION_EXIT_PHRASES) / config command_mode.abort_phrases
